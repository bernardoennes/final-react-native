import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  card: {
    width: 60,
    height: 90,
    borderRadius: 6,
    borderWidth: 1,
    borderColor: "#fff",
  },
});

export default styles;
